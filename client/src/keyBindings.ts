interface KeyBindings{[key:string]:string};

export var keyBindings:KeyBindings = {
    FORWARD : "w",
    BACK : "s",
    LEFT : "a",
    RIGHT : "d",
    SPRINT : "Shift",
    JUMP : "Space",
}
